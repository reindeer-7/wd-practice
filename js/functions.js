function name(params) {
  // To do
}

// Par'ametros o argumentos
function suma(a, b) { }
function getName(dni) { }

/**
 * H: Create a func to know if a city could be flood.
 * ---
 * juliaca: Yes
 * puno: No
 * ilave: No
 * juli: No
 *
 * true/false
 */
function isFlood(city) {}

/**
 * L: Create a func to get the price of chiken based on the weight;
 * ---
 * Chicken price 12.30 per Kilogram.
 */
function getChikenPrice(weight) {
}

/**
 * P: Create a func to get temperature based on a province code.
 * ---
 * pu: 10.5
 * co: 12.5
 * de: 15.6
 * az: 10.2
 */
function getTemperature(provinceCode) {}


/**
 * Y: Create a func to get the currency exchange
 *
 * usd -> 3.82
 * eur -> 4.02
 *
 * "usd", 100 -> 382
 * "eur", 50 -> 201
 */

function getExchange(currencyType, valueToChange) {}

/**
 * Yu: Create a func to get fee based on my salary.
 * --
 * Fee 8%
 *
 * 1000 -> 80
 * 2000 -> 160
 */
function getFee(salary) {}