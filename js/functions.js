function name(params) {
  // To do
}

// Par'ametros o argumentos
function suma(a, b) { }
function getName(dni) { }

// H: Create a func to know if a city could be flood.
function isFlood(city) {
  return true; // true/false
}

// L: Create a func to get the price of chiken based on the weight;
function getChikenPrice(weight) {
  return 120.25; // Number
}

// P: Create a func to get temperature based on a province.
function getTemperature(province) {
  const temperature = 15.6;
  return temperature; // Number
}

// Y: Create a func to get the USD exchange value
function getUSDExchange() {
  return { exchange: 3.82 }; // Number
}

getUSDExchange();

// Yu: Create a func to get fee based on my salary.
function getFee(salary) {
  return [45, 16];
}