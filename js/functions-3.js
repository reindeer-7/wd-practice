/**
 * [2, 5] -> 7
 */

function sumArray(arr) {
  return arr[0] + arr[1];
}


function sum(arr) {
  let m = arr[0] + arr[0];
  return m;
}

// sumArray([2, 5]) // 7
















/**
 * ala     -> 3
 * vamos   -> 4
 * casaca  -> 6
 */

/**
 * ala     -> ALA
 * vamos   -> VAMOS
 * casaca  -> CASACA
 */

/**
 * ALA     -> ala
 * VAMOS   -> vamos
 * CASACA  -> casaca
 */